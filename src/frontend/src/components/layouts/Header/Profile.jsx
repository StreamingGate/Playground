import React from 'react';

import { IconButton } from '@components/buttons';
import { Alarm } from '@components/cores';

function Profile() {
  return (
    <IconButton>
      <Alarm />
    </IconButton>
  );
}

export default Profile;
